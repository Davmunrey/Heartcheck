# Eval datasets (real photos never committed; synth regenerated locally)
data/raw/
data/real_eval/images/
data/real_eval/manifest.jsonl
data/synth_*/
runs/auto/
apps/ml-api/weights/

# Local DB and venv
apps/ml-api/data/*.db
apps/ml-api/.venv/
.venv/

# API secrets (use apps/ml-api/.env.example as template)
apps/ml-api/.env
apps/ml-api/.env.local

# Web app secrets
apps/web/.env
apps/web/.env.local
apps/web/.env*.local
!apps/web/.env.example

# Infra secrets
infra/.env
infra/.env.local

# Reports
eval/reports/

# Turborepo (local cache; not for VCS)
.turbo/
**/.turbo/

# Python packaging / TypeScript build info
*.egg-info/
*.tsbuildinfo

# Codegen intermediate artifact (regenerated by `npm run codegen`)
openapi.json

# OS / IDE noise
.DS_Store
*.pyc
__pycache__/
.pytest_cache/
node_modules/
dist/
runs/local/
.gstack/

# Local Supabase CLI scaffold (canonical migrations live in infra/supabase/)
# Root-anchored so it does NOT match infra/supabase/.
/supabase/
